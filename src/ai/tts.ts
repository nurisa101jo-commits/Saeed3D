import type {TTSProvider} from '../types/api'; export const createRemoteTTS=(url:string,key?:string):TTSProvider=>({async synthesize(text){const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json',...(key?{Authorization:`Bearer ${key}`}:{})},body:JSON.stringify({text})});if(!r.ok)throw new Error(`TTS ${r.status}`);return{audio:await r.arrayBuffer(),mimeType:r.headers.get('content-type')||'audio/wav'}}});
