import {useEffect,useState} from 'react';
export function UpdateStatus(){const[s,setS]=useState<any>({state:'idle'});useEffect(()=>{window.electronAPI.getUpdateState().then(setS);return window.electronAPI.onUpdate(setS)},[]);if(['idle','current'].includes(s.state))return null;return <div className="updateStatus"><b>{s.state==='checking'?'Checking…':s.state==='available'?'Update available':s.state==='downloading'?'Updating…':s.state==='downloaded'?'Update ready':s.state==='error'?'Update error':s.state}</b>{s.total?<span>{s.percent?.toFixed(0)}% · {(s.transferred/1048576).toFixed(1)} / {(s.total/1048576).toFixed(1)} MB</span>:s.message&&<span>{s.message}</span>}{s.state==='downloaded'&&<button onClick={()=>window.electronAPI.installUpdate()}>Restart & install</button>}</div>}