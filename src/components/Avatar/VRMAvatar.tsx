import {useEffect,useRef} from 'react';import * as THREE from 'three';import {GLTFLoader} from 'three/examples/jsm/loaders/GLTFLoader.js';import {VRMLoaderPlugin,VRM} from '@pixiv/three-vrm';import {setEmotion} from '../../avatar/expressions';
export function VRMAvatar(){const ref=useRef<HTMLDivElement>(null);useEffect(()=>{const host=ref.current;if(!host)return;const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(24,1,.01,100);const renderer=new THREE.WebGLRenderer({alpha:true,antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,1.5));renderer.setSize(host.clientWidth,host.clientHeight);host.appendChild(renderer.domElement);scene.add(new THREE.HemisphereLight(0xffffff,0x444444,1.8));const loader=new GLTFLoader();loader.register(p=>new VRMLoaderPlugin(p));let vrm:VRM|undefined;function fit(){if(!vrm)return;const box=new THREE.Box3().setFromObject(vrm.scene);const size=box.getSize(new THREE.Vector3());const center=box.getCenter(new THREE.Vector3());const h=Math.max(size.y,0.1);const fov=THREE.MathUtils.degToRad(camera.fov);const dist=(h*.62)/Math.tan(fov/2);camera.position.set(center.x,center.y+h*.03,center.z+dist);camera.lookAt(center.x,center.y+h*.03,center.z);camera.aspect=Math.max(1,host.clientWidth/Math.max(1,host.clientHeight));camera.updateProjectionMatrix()}loader.load('./models/saeed.vrm',g=>{vrm=g.userData.vrm;scene.add(vrm.scene);vrm.scene.rotation.y=Math.PI;setEmotion(vrm,'neutral');fit()});const ro=new ResizeObserver(()=>{renderer.setSize(host.clientWidth,host.clientHeight);fit()});ro.observe(host);let id=0;const loop=()=>{id=requestAnimationFrame(loop);vrm?.update(1/60);renderer.render(scene,camera)};loop();return()=>{cancelAnimationFrame(id);ro.disconnect();renderer.dispose();host.removeChild(renderer.domElement)}},[]);return <div ref={ref} style={{position:'absolute',inset:0}}/>}