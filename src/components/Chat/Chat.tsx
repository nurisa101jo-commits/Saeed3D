import {useEffect,useRef,useState} from 'react';import {ConversationManager,runCommand} from '../../ai/chat';import {buildPrompt} from '../../ai/prompts';import {AlwaysListener} from '../../audio/alwaysListening';
export function Chat(){const[text,setText]=useState('');const[answer,setAnswer]=useState('');const[listening,setListening]=useState(false);const[error,setError]=useState('');const listener=useRef<AlwaysListener>();const speakQueue=useRef(Promise.resolve());async function speak(t:string){speakQueue.current=speakQueue.current.then(async()=>{try{const b=await window.electronAPI.speak(t);const u=URL.createObjectURL(new Blob([Uint8Array.from(atob(b),c=>c.charCodeAt(0))],{type:'audio/mpeg'}));const a=new Audio(u);await a.play();await new Promise(r=>{a.onended=()=>r(null)})}catch(e){setError(String(e))}})}
async function send(q=text){if(!q.trim())return;setText('');setAnswer('');setError('');if(await runCommand(q))return;const manager=new ConversationManager();let sentence='';await manager.run(buildPrompt(q),d=>{setAnswer(x=>x+d);sentence+=d;if(/[.!?؟]\s*$/.test(sentence)){const s=sentence;sentence='';void speak(s)}}).catch(e=>setError(String(e)));}
async function start(){if(listener.current)return;try{const l=new AlwaysListener();listener.current=l;await l.start(async b=>{setListening(true);try{const q=await window.electronAPI.transcribe(await b.arrayBuffer());if(q)await send(q)}catch(e){setError(String(e))}finally{setListening(false)}});setListening(true)}catch(e){setError('Microphone permission is required by Windows: '+String(e))}}
useEffect(()=>{start();return()=>listener.current?.stop()},[]);return <div className="chat"><div className="listen">{listening?'● Listening':'○ Mic ready'}</div><div className="answer">{answer}</div>{error&&<div className="error">{error}</div>}<div className="row"><input value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Talk to Saeed..."/><button onClick={()=>send()}>Send</button></div></div>}