import {useState} from 'react'; import {ConversationManager,localDemo} from '../../ai/chat'; import {buildPrompt} from '../../ai/prompts'; export function Chat(){const [text,setText]=useState('');const [answer,setAnswer]=useState('');const send=async()=>{if(!text.trim())return;const q=text;setText('');setAnswer('');await new ConversationManager({chat:localDemo as any}).run(buildPrompt(q),d=>setAnswer(x=>x+d))};return <div className="chat"><div className="answer">{answer}</div><div className="row"><input value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Talk to Saeed..."/><button onClick={send}>Send</button></div></div>}
