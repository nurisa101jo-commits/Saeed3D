export class Recorder{private r?:MediaRecorder; private chunks:Blob[]=[]; async start(){const stream=await navigator.mediaDevices.getUserMedia({audio:true});this.chunks=[];this.r=new MediaRecorder(stream);this.r.ondataavailable=e=>e.data.size&&this.chunks.push(e.data);this.r.start();return stream} stop():Promise<Blob>{return new Promise(resolve=>{if(!this.r)return resolve(new Blob());this.r.onstop=()=>resolve(new Blob(this.chunks,{type:this.r?.mimeType||'audio/webm'}));this.r.stop()})}}
