import {app,BrowserWindow,Menu,Notification,Tray,nativeImage} from 'electron';
import electronUpdater from 'electron-updater';
const {autoUpdater}=electronUpdater; import path from 'node:path'; import {fileURLToPath} from 'node:url';
type U={state:string;version?:string;percent?:number;transferred?:number;total?:number;message?:string};let tray:Tray|undefined;let win:BrowserWindow|undefined;let state:U={state:'idle'};const root=path.dirname(fileURLToPath(import.meta.url));
function emit(){win?.webContents.send('update:status',state)}function set(s:U){state=s;emit();tray?.setToolTip('Saeed — '+(s.message||s.state))}
function reveal(){if(!win||win.isDestroyed())return;win.show();win.focus();win.setAlwaysOnTop(true,'floating')}
export function setupUpdater(w:BrowserWindow){win=w;app.setAppUserModelId('com.saeed.desktop');const iconPath=app.isPackaged?path.join(process.resourcesPath,'icons','saeed.png'):path.join(root,'../../public/icons/saeed.png');const icon=nativeImage.createFromPath(iconPath);tray=new Tray(icon);tray.setContextMenu(Menu.buildFromTemplate([{label:'Show Saeed',click:()=>reveal()},{label:'Check for updates',click:()=>{reveal();void check()}},{label:'Install downloaded update',enabled:false},{type:'separator'},{label:'Quit Saeed',click:()=>app.quit()}]));autoUpdater.autoDownload=true;autoUpdater.autoInstallOnAppQuit=true;autoUpdater.on('checking-for-update',()=>set({state:'checking',message:'Checking for updates…'}));autoUpdater.on('update-available',i=>{reveal();set({state:'available',version:i.version,message:'Update available: '+i.version});tray?.displayBalloon({title:'Saeed update available',content:'Version '+i.version+' is being downloaded.'});new Notification({title:'Saeed update available',body:'Version '+i.version+' is being downloaded.'}).show()});autoUpdater.on('download-progress',p=>set({state:'downloading',version:state.version,percent:p.percent,transferred:p.transferred,total:p.total,message:`Downloading ${p.percent.toFixed(0)}% — ${(p.transferred/1048576).toFixed(1)} / ${(p.total/1048576).toFixed(1)} MB`}));autoUpdater.on('update-downloaded',i=>{reveal();set({state:'downloaded',version:i.version,percent:100,message:'Update downloaded and ready to install'});tray?.displayBalloon({title:'Saeed update ready',content:'The update is ready to install.'});new Notification({title:'Saeed update ready',body:'The update is ready to install.'}).show()});autoUpdater.on('update-not-available',()=>set({state:'current',message:'Saeed is up to date'}));autoUpdater.on('error',e=>{reveal();set({state:'error',message:e.message})});setTimeout(()=>{if(app.isPackaged)check()},8000)}
export async function check(){reveal();set({state:'checking',message:'Checking for updates…'});try{await autoUpdater.checkForUpdates()}catch(e){set({state:'error',message:String(e)})}}export function install(){if(state.state==='downloaded')autoUpdater.quitAndInstall()}export function current(){return state}