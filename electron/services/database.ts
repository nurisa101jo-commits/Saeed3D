import DatabaseDriver from 'better-sqlite3';
export class Database{private db:DatabaseDriver.Database;constructor(file:string){this.db=new DatabaseDriver(file);this.db.pragma('journal_mode=WAL');this.db.exec(`CREATE TABLE IF NOT EXISTS settings(key TEXT PRIMARY KEY,value TEXT NOT NULL);CREATE TABLE IF NOT EXISTS memories(id INTEGER PRIMARY KEY AUTOINCREMENT,content TEXT NOT NULL,importance REAL DEFAULT .5,created_at TEXT NOT NULL);CREATE TABLE IF NOT EXISTS messages(id INTEGER PRIMARY KEY AUTOINCREMENT,role TEXT NOT NULL,content TEXT NOT NULL,created_at TEXT NOT NULL);`)}getSetting(k:string){const r=this.db.prepare('SELECT value FROM settings WHERE key=?').get(k) as any;return r?JSON.parse(r.value):null}setSetting(k:string,v:unknown){this.db.prepare('INSERT INTO settings(key,value) VALUES(?,?) ON CONFLICT(key) DO UPDATE SET value=excluded.value').run(k,JSON.stringify(v))}deleteSetting(k:string){this.db.prepare('DELETE FROM settings WHERE key=?').run(k)}addMemory(m:any){const r=this.db.prepare('INSERT INTO memories(content,importance,created_at) VALUES(?,?,?)').run(String(m.content),Number(m.importance??.5),new Date().toISOString());return r.lastInsertRowid}searchMemory(q:string){if(!q.trim())return this.db.prepare('SELECT * FROM memories ORDER BY id DESC LIMIT 50').all();return this.db.prepare('SELECT * FROM memories WHERE content LIKE ? ORDER BY importance DESC,id DESC LIMIT 20').all('%'+q+'%')}}