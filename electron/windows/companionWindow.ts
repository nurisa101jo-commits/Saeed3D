import {BrowserWindow,screen} from 'electron';import path from 'node:path';export function revealCompanionWindow(win:BrowserWindow){if(win.isDestroyed())return;const d=screen.getPrimaryDisplay();const b=win.getBounds();if(b.x+b.width<=d.workArea.x||b.x>=d.workArea.x+d.workArea.width||b.y+b.height<=d.workArea.y||b.y>=d.workArea.y+d.workArea.height)win.setPosition(d.workArea.x+d.workArea.width-440,d.workArea.y+d.workArea.height-690);if(win.isMinimized())win.restore();win.setOpacity(1);win.setAlwaysOnTop(true,'floating');win.show();win.moveTop()}export function createCompanionWindow(root:string){const d=screen.getPrimaryDisplay();const win=new BrowserWindow({width:420,height:650,x:d.workArea.x+d.workArea.width-440,y:d.workArea.y+d.workArea.height-690,frame:false,transparent:true,backgroundColor:'#00000000',resizable:true,hasShadow:false,alwaysOnTop:true,skipTaskbar:false,show:true,webPreferences:{preload:path.join(root,'preload.mjs'),nodeIntegration:false,contextIsolation:true,sandbox:false,backgroundThrottling:false}});win.setAlwaysOnTop(true,'floating');const reveal=()=>revealCompanionWindow(win);win.webContents.on('did-finish-load',reveal);win.webContents.on('did-fail-load',()=>reveal());win.webContents.on('render-process-gone',()=>{if(!win.isDestroyed())setTimeout(reveal,100)});const indexPath=path.join(root,'../dist/index.html');const p=process.env.VITE_DEV_SERVER_URL?win.loadURL(process.env.VITE_DEV_SERVER_URL):win.loadFile(indexPath);p.catch(e=>console.error('[Saeed] window load error:',e));return win;}