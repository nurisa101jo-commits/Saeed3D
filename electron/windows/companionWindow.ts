import {BrowserWindow,screen} from 'electron'; import path from 'node:path';
export function createCompanionWindow(root:string){const display=screen.getPrimaryDisplay(); const win=new BrowserWindow({width:420,height:650,x:display.workArea.x+display.workArea.width-440,y:display.workArea.y+display.workArea.height-690,frame:false,transparent:true,backgroundColor:'#00000000',resizable:true,hasShadow:false,alwaysOnTop:true,skipTaskbar:false,webPreferences:{preload:path.join(root,'preload.js'),nodeIntegration:false,contextIsolation:true,sandbox:true}}); if(process.env.VITE_DEV_SERVER_URL) win.loadURL(process.env.VITE_DEV_SERVER_URL); else win.loadFile(path.join(root,'../dist/index.html')); return win;}
